--- a/pyproject.toml
+++ b/pyproject.toml
@@ -1,5 +1,5 @@
 [build-system]
-requires = ["setuptools>=42", "setuptools_scm[toml]>=7"]
+requires = ["setuptools>=42"]
 build-backend = "setuptools.build_meta"

 [tool.setuptools_scm]
+version_scheme = "release-branch-semver"
+local_scheme = "node-distance"
